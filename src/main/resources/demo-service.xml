<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-file="http://www.springframework.org/schema/integration/file"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/integration https://www.springframework.org/schema/integration/spring-integration-5.2.xsd
           http://www.springframework.org/schema/integration/file https://www.springframework.org/schema/integration/file/spring-integration-file-5.2.xsd">

    <!-- Load external properties -->
    <context:property-placeholder location="classpath:app.properties"/>

    <!-- DataSource using HikariCP -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource">
        <property name="poolName" value="DemoHikariCP"/>
        <property name="maximumPoolSize" value="${hikari.maximumPoolSize:5}"/>
        <property name="minimumIdle" value="${hikari.minimumIdle:1}"/>
        <property name="connectionTimeout" value="${hikari.connectionTimeout:30000}"/>
        <property name="jdbcUrl" value="jdbc:h2:file:${user.home}/demo-db;DB_CLOSE_DELAYED=1000;DB_CLOSE_TIMEOUT=5000;mode=MySQL"/>
        <property name="username" value="${db.username:sa}"/>
        <property name="password" value="${db.password:}"/>
    </bean>

    <!-- JdbcTemplate -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="dataSource"/>
    </bean>

    <!-- Database schema initialization -->
    <bean id="databaseInitializer" class="org.springframework.jdbc.datasource.init.ResourceDatabasePopulator">
        <property name="scripts">
            <list>
                <value>classpath:db-schema.sql</value>
            </list>
        </property>
    </bean>

    <!-- Transformer to convert File to String and add file_name header -->
    <bean id="fileToMessageTransformer" class="de.antrophos.spring.demo.FileToMessageTransformer"/>

    <!-- Bean for writing file data to DB -->
    <bean id="fileMessageWriter" class="de.antrophos.spring.demo.FileMessageWriter">
        <property name="jdbcTemplate" ref="jdbcTemplate"/>
    </bean>

    <!-- Channel definitions -->
    <int:channel id="fileInputChannel"/>

    <!-- File polling adapter -->
    <int-file:inbound-channel-adapter id="filePoller"
        directory="${input.dir}"
        filename-pattern="${file.pattern}"
        channel="fileInputChannel"
        auto-create-directory="true">
        <int:poller ref="fileToMessageTransformer"/>
    </int-file:inbound-channel-adapter>

    <!-- Service activator that writes file data to DB -->
    <int:service-activator input-channel="fileInputChannel"
        ref="fileMessageWriter"
        method="write"/>
</beans>